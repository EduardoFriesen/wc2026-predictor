# AGENTS.md

## State

- **Remote:** `https://github.com/EduardoFriesen/wc2026-predictor.git`
- **Project guess (from name):** World Cup 2026 predictor application
- **Current branch:** `main` (no commits yet)

## Config

- `opencode.json` at root — lists `@plannotator/opencode` plugin (auto-added `$schema`).
- `.opencode/opencode.json` — created by `opencode plugin install`, same plugin entry.

## Language

Respondir siempre en español.

## Before working

1. Run `git status` and `git log` to check for any new commits.
2. Look for `README*`, `package.json`, workspace config, or any tool config before assuming conventions.
3. No build, test, lint, or format commands exist yet — do not guess them.
